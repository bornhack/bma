"""AppConfig for the frontpage app."""

from django.apps import AppConfig


class FrontpageConfig(AppConfig):
    """AppConfig for the frontpage app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "frontpage"
