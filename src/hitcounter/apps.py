"""AppConfig for the hitcounter model."""

from django.apps import AppConfig


class HitcounterConfig(AppConfig):
    """AppConfig for the hitcounter model."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "hitcounter"
