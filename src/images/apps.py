"""AppConfig for the images app."""

from django.apps import AppConfig


class ImagesConfig(AppConfig):
    """AppConfig for the images app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "images"
