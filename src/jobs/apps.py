"""AppConfig for the jobs app."""

from django.apps import AppConfig


class JobsConfig(AppConfig):
    """AppConfig for the jobs app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "jobs"
