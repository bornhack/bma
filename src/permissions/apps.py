"""AppConfig for the permissios app."""

from django.apps import AppConfig


class PermissionsConfig(AppConfig):
    """AppConfig for the permissios app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "permissions"
